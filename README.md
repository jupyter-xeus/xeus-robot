# xeus-robot


```
conda install -c conda-forge xeus-python xtl cmake cppzmq nlohmann_json pybind11 pip robotframework
pip install robotframework-interpreter
cmake -D CMAKE_INSTALL_PREFIX=$CONDA_PREFIX .
make install -j6
```
